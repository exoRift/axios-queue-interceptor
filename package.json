{
  "name": "axios-queue-interceptor",
  "version": "1.1.3",
  "description": "A per-host queue manager for Axios",
  "keywords": [
    "axios",
    "queue",
    "ratelimit"
  ],
  "files": [
    "dist/"
  ],
  "author": {
    "name": "exoRift",
    "url": "https://github.com/exoRift"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/exoRift/axios-queue-interceptor.git"
  },
  "module": "index.ts",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "[ -d dist ] && rm -r dist; bun build --target=browser --format=esm --packages external src/index.ts --outfile dist/index.js && tsc --project tsconfig.build.json",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "eslint": "^9.31.0",
    "eslint-config": "github:exoRift/eslint-config",
    "eslint-plugin-jsdoc": "^51.3.4"
  },
  "peerDependencies": {
    "axios": "^1.10.0",
    "typescript": "^5"
  },
  "dependencies": {
    "@datastructures-js/priority-queue": "^6.3.3"
  }
}
